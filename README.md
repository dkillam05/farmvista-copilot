FarmVista Copilot v2 (Phase 1)

DB-first. No fallbacks. No domains. No magic.

If this answers a question, it came from:
SQLite → Getter → Formatter → OpenAI wording.

Required env:
- OPENAI_API_KEY
- SNAPSHOT_SQLITE_PATH
